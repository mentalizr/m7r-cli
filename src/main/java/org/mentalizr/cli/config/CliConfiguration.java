package org.mentalizr.cli.config;

import org.mentalizr.client.ClientConfiguration;

public class CliConfiguration extends ClientConfiguration {

    public CliConfiguration(String server) {
        super(server);
    }

}
